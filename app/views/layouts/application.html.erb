<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title><%= page_title %></title>

    <%= stylesheet_link_tag "http://fonts.googleapis.com/css?family=Raleway:300,700", media: "all" %>
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield :head %>
    <%= javascript_include_tag "stretchy", async: true, "data-filter" => "textarea" %>

    <link rel="icon" type="image/png" href="<%= image_url("favicon.png") %>">
    <link rel="icon" type="image/png" href="<%= image_url("favicon@2x.png") %>">
  </head>
  <body>
    <div class="page-container">
      <%= render partial: "shared/sidebar" %>
      <%= render partial: "shared/header" %>

      <main>
        <%= flash_messages %>
        
        <%= yield %>
      </main>
    </main>
  </body>
</html>
