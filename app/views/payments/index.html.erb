<div class="payments-list">
  <header>
    
  </header>
  <div class="table-container">
    <div class="table-and-footer" id="table">
      <div class="scrolling-table">
        <table>
          <thead>
            <tr>
              <th><input type="checkbox" name="select_all"></th>
              <th><%= User.human_attribute_name(:name) %></th>
              <th><%= Payment.human_attribute_name(:created_at) %></th>
              <th><%= Payment.human_attribute_name(:amount) %></th>
              <th><%= Payment.human_attribute_name(:payment_method_name) %></th>
              <th><%= Payment.human_attribute_name(:state) %></th>
            </tr>
          </thead>
          <tbody>
            <%= render(@payments) %>
          </tbody>
        </table>
      </div>
      <footer>
        <div class="buttons">
          <button rel="approve">
            <%= icon :ok %>
            <span><%= t("payments.approve") %></span>
          </button>
          <button rel="decline">
            <%= icon :cancel %>
            <span><%= t("payments.decline") %></span>
          </button>
        </div>
        <%= will_paginate @payments, inner_window: 2, outer_window: 0 %>
      </footer>
    </div>
  </div>
</div>
